{{#if isJsCode}}
(() => {
  const sdk = window.ugc.getWidgetBySelector(`{{{selector}}}`).getSdk();
  sdk.querySelector('#nosto-ugc-container').style.display = 'none';
  sdk.addEventListener('load', () => {
    sdk.querySelector('#nosto-ugc-container').style.display = '';
  });
  {{{code}}}
})();
{{else}}
    {{{code}}}
{{/if}}